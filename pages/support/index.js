import { Layout } from '@my-components';
import { Support } from '@pages/public/support';

const HomePage = () => {
  return (
    <Layout>
      <Support />
    </Layout>
  );
};
export default HomePage;
