import { Layout } from '@my-components';
import { Contact } from '@pages/public/contact';

const HomePage = () => {
  return (
    <Layout>
      <Contact />
    </Layout>
  );
};
export default HomePage;
