import { Layout } from '@my-components';
import { AboutUs } from '@pages/public/about-us';

const HomePage = () => {
  return (
    <Layout>
      <AboutUs />
    </Layout>
  );
};
export default HomePage;
